[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
    "python -m venv .venv",
    ". .venv/bin/activate && pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = ". .venv/bin/activate && python bot_analyzer.py"
